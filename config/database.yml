defaults: &defaults
  encoding: unicode
  adapter: postgresql
  username: checkin
  password:
  database: checkin_development

production:
  <<: *defaults

development:
  <<: *defaults

test:
  <<: *defaults

staging:
  <<: *defaults
